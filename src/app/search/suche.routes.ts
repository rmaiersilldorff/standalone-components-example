import {Routes} from '@angular/router';
import {SuchePageComponent} from './suche-page/suche-page.component';

export const sucheRoutes: Routes = [
    {path: '', component: SuchePageComponent}
];

export default sucheRoutes;
