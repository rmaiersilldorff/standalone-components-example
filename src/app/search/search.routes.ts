import {Routes} from '@angular/router';
import {SuchePageComponent} from './search-page/suche-page.component';

export const searchRoutes: Routes = [
    {path: '', component: SuchePageComponent}
];

export default searchRoutes;
